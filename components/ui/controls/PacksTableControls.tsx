"use client";
import { Button } from "@radix-ui/themes";
import { useDispatch } from "react-redux";

export const PacksTableON = () => {
  const dispatch = useDispatch();
  return (
    <>
      {/* <Button onClick={()=>dispatch()}>Détails</Button> */}
    </>
  );
};
export const PacksTableOFF = () => {};
