import Section from "@/components/layout/Section";
import SpeakersGrid from "./element/SpeakersGrid";

const Speakers = () => {
  return (
    <Section id="speakers" title="Intervenants" withTexture>
      <SpeakersGrid />
    </Section>
  );
};

export default Speakers;
